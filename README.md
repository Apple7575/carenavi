# CareNavi - 가족 건강 관리 플랫폼

부모님과 가족의 건강을 체계적으로 관리하는 웹 플랫폼

## 주요 기능

### 🏥 건강 지표 추적
- 혈압, 혈당, 체중, 심박수 기록
- 시각화된 건강 추이 차트
- 유형별 필터링

### 💊 복약 관리
- 복약 일정 등록 및 알림
- 복약 순응도 추적
- 복약 완료 체크

### 👨‍👩‍👧‍👦 가족 관리
- 가족 그룹 생성 및 초대
- 초대 코드로 간편 참여
- 가족 구성원별 건강 점수

### ✅ 할 일 관리
- 검진, 운동 등 건강 관련 할 일
- 우선순위 및 카테고리 분류
- 마감일 추적

### 📦 제품 재고 관리
- 의료용품 및 생필품 재고 추적
- 유통기한 알림
- 재고 수량 관리

### 📊 대시보드
- 가족 전체 건강 상태 한눈에 보기
- 오늘의 복약 일정
- 건강 지표 추이

## 기술 스택

- **Frontend**: Next.js 14, React 18, TypeScript
- **Styling**: Tailwind CSS, Radix UI
- **State Management**: TanStack Query (React Query v5)
- **Database**: Supabase (PostgreSQL)
- **Auth**: Supabase Auth
- **Charts**: Recharts
- **Icons**: Lucide React
- **Validation**: Zod

## 시작하기

### 필수 요구사항

- Node.js 18+
- npm 또는 yarn
- Supabase 계정 (로컬 개발용)

### 설치

```bash
# 의존성 설치
npm install

# 환경 변수 설정
cp .env.example .env.local
# .env.local 파일에 Supabase 정보 입력

# Supabase 로컬 개발 환경 시작
supabase init
supabase start

# 마이그레이션 실행
supabase db push

# 개발 서버 실행
npm run dev
```

### 빌드

```bash
# 프로덕션 빌드
npm run build

# 프로덕션 서버 실행
npm start
```

## 프로젝트 구조

```
carenavi/
├── app/                      # Next.js App Router
│   ├── (auth)/              # 인증 관련 페이지
│   ├── (dashboard)/         # 대시보드 페이지
│   ├── api/                 # API 라우트
│   ├── layout.tsx           # 루트 레이아웃
│   └── page.tsx             # 홈 페이지
├── components/              # React 컴포넌트
│   ├── ui/                  # 공통 UI 컴포넌트
│   ├── dashboard/           # 대시보드 컴포넌트
│   ├── family/              # 가족 관리 컴포넌트
│   ├── medications/         # 복약 관리 컴포넌트
│   ├── vitals/              # 건강 지표 컴포넌트
│   ├── tasks/               # 할 일 컴포넌트
│   ├── products/            # 제품 관리 컴포넌트
│   ├── layout/              # 레이아웃 컴포넌트
│   └── shared/              # 공유 컴포넌트
├── hooks/                   # Custom React Hooks
├── lib/                     # 유틸리티 라이브러리
│   ├── supabase/           # Supabase 클라이언트
│   ├── react-query/        # React Query 설정
│   └── utils/              # 유틸리티 함수
├── supabase/               # Supabase 설정
│   └── migrations/         # 데이터베이스 마이그레이션
├── styles/                 # 글로벌 스타일
├── types/                  # TypeScript 타입 정의
└── public/                 # 정적 파일
```

## 데이터베이스 스키마

### 주요 테이블

- `users` - 사용자 정보
- `families` - 가족 그룹
- `family_members` - 가족 구성원
- `medications` - 복약 정보
- `medication_logs` - 복약 기록
- `vitals` - 건강 지표
- `tasks` - 할 일
- `products` - 제품 재고

### Row Level Security (RLS)

- 모든 테이블에 RLS 정책 적용
- 가족 단위 데이터 격리
- 사용자별 권한 관리

## 개발 가이드

### 새로운 기능 추가

1. **API 라우트 생성** (`app/api/[feature]/route.ts`)
2. **React Query 훅 작성** (`hooks/use[Feature].ts`)
3. **UI 컴포넌트 개발** (`components/[feature]/`)
4. **페이지 생성** (`app/(dashboard)/[feature]/page.tsx`)

### 코드 스타일

- TypeScript strict 모드 사용
- Tailwind CSS 유틸리티 클래스 우선
- CSS 변수는 `styles/global.css`에 정의
- 컴포넌트는 함수형 + React Hooks 패턴

## 배포

### Vercel 배포

```bash
# Vercel CLI 설치
npm i -g vercel

# 배포
vercel
```

### 환경 변수 설정

Vercel 대시보드에서 다음 환경 변수 설정:

- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- `NEXT_PUBLIC_APP_URL`

## 라이선스

MIT License

## 기여

이슈와 PR을 환영합니다!

## 지원

문제가 발생하면 [Issues](https://github.com/yourusername/carenavi/issues)에 등록해 주세요.
